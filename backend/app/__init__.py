"""Backend application package for the sports card image info extractor.

Phase 1+ will add the FastAPI app, the Claude extraction service, enrichment
(PSA cert lookup, checklist matching), and normalization. For now this package
exposes the generated `CardInfo` data models (see `app.models`).
"""
